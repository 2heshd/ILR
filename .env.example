# Optional: enables cross-device cloud sync + magic-link login.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: enables automatic advanced-word generation, DLI definition/romanization enrichment,
# adaptive reading/listening passages, and high-quality Persian TTS.
# Keep this server-side. Never prefix it with NEXT_PUBLIC_.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6
